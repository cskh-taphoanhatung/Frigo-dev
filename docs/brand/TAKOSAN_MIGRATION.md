# Takosan brand migration (Frigo → Takosan, user-facing only)

Independent branding branch descending from the T13 certified lineage. It is
**not** T13 remediation, not T14, and not production reconciliation. The
certified T13 application freeze `32ddbb4f2bb636fdcf201e9ca99c4689d3655477`, its
docs head `42e0037f…` and continuation `897102b6…` are untouched; independent T13
Review #2 must still review exact freeze `32ddbb4`.

| Key | Value |
| --- | --- |
| Repository ID | 1368281478 (`vn-blo/Frigo-dev`) |
| Brand branch | `hoplite/megara-hyblaia-6b723eb2` (Hoplite broker-authorized name; `feat/takosan-brand-refresh` was the preferred name but the broker only publishes the thread branch) |
| TAKOSAN_BRAND_BASE | `897102b6816c22af2e6a49f29662690e3e3206e0` |
| TAKOSAN_BRAND_APPLICATION_CHECKPOINT | `e37ee2808a50a7195dc90a2e7bb01be639aa186b` |
| TAKOSAN_BRAND_DOCS_HEAD | the docs-only commit that adds this file |
| Protected main | `d1b06732f8a80db4e77986df31ff28d9f04641fa` (unchanged) |

## Source of truth

The supplied `takosan-brand-kit.zip` (tokens v1.1.0) is the only visual source.
Logos, wordmark, symbol, mascot poses and UI icons are the kit SVGs installed
verbatim; nothing was redrawn, typeset or generated by an image model. Palette:
coral `#FF7B6B`, green `#2E7D5B`, navy `#1F2937`, cream `#FFF8F3`, mint
`#DFF4E6`, yellow `#FFC857`. Semantic: primary action = green, accent = coral,
text = navy, surface = cream. Typography: Nunito (Google Fonts, same hosted
pattern the app already used) with `system-ui, sans-serif` fallback; the
TAKOSAN wordmark is always the supplied SVG geometry.

## Runtime asset layout

Only the assets the app uses were installed (≈230 KB); design/marketing/social
templates stay in the kit.

```
public/takosan/brand/      horizontal + stacked lockups (primary/white), wordmark, symbol, symbol-micro, takosan-og.png
public/takosan/app-icons/  favicon.svg, adaptive fg/bg SVG, icon-{16..512}.png, icon-maskable-512.png
public/takosan/mascot/     neutral, wave, thinking, celebrate, fridge, recipe, cooking, calendar, shopping (SVG)
public/takosan/ui-icons/   home, fridge, scan, recipe, meal-plan, calendar, shopping-list, profile (SVG)
```

PNG icons are derived deterministically from the kit's 1024px
`takosan-app-icon-light.png` by `scripts/generate-takosan-icons.mjs` (sharp,
lanczos3). The maskable icon composites the kit adaptive background + foreground
so the mascot stays inside the 80 % safe area. The OpenGraph card rasterizes the
kit horizontal-lockup SVG onto a 1200×630 cream canvas. Re-run with
`NODE_PATH=node_modules/.pnpm/sharp@0.33.5/node_modules node scripts/generate-takosan-icons.mjs <kit-dir>`.

`public/frigo/ingredients` and `public/frigo/recipes` are content, not brand, and
were not moved. `public/frigo/{brand,app-icons,illustrations}` remain on disk
for now (no runtime references from the primary shell) and can be removed once
the migration is accepted.

## Code contract

- `src/web/lib/takosan-brand.ts` — `TAKOSAN_BRAND` (name, tagline, logos,
  wordmark, symbol, mascot, appIcons, uiIcons, colors). Components read brand
  paths only from here. `FRIGO_ASSETS` is unchanged and still owns ingredient /
  recipe content paths.
- `src/web/components/common/TakosanIcon.tsx` — inline SVG renderer for the kit
  UI icon grammar (24×24, currentColor, 1.8 round stroke). Used in BottomNav
  (home/fridge/scan/meal-plan/profile) and Landing CTAs. Generic system icons
  (back, close, bell, settings, chevrons…) stay Lucide.
- `src/web/styles/takosan-tokens.css` — `--takosan-*` and semantic
  `--color-action-primary`, `--color-action-accent`, `--color-text-primary`,
  `--color-surface`. `frigo-tokens.css` now imports it and keeps every
  `--frigo-*` name as a compatibility alias (do not delete until proven).
- `tailwind.config.js` — `takosan.{coral,green,navy,cream,mint,yellow}` colours
  and Nunito for `heading`/`body`/`sans`. The legacy `frigo.*` palette is kept.

## Surfaces changed

index.html (title, description, favicon SVG + PNG, apple-touch-icon 180,
theme-color `#2E7D5B`, OG title/description/image, Nunito), `manifest.json`
(name/short_name Takosan, icons, theme/background), `sw.js` (`takosan-pwa-v2`,
Takosan precache, `/takosan/` cache-first; old caches are evicted on activate by
the existing non-destructive `keys().filter` logic), Landing, Onboarding (splash
= navy + white lockup + wave mascot; welcome = cooking; done = celebrate), Auth,
Header/TopBar (horizontal lockup), BottomNav (Takosan icons, green scan CTA,
mint selected state), Button (green primary / mint secondary), EmptyState (legacy
illustration types → mascot poses: fridge / recipe / shopping / cooking /
thinking), RouteFallback (symbol), Home hero, WeekGenerating (calendar mascot),
WeekShopping + CookingComplete success (celebrate), Scan camera + OfflineBanner
(navy), Plus paywall, Settings, Profile, Notifications, Family sharing, Week
setup/dashboard/export, planner copy, VietQR modal copy, auth default display
names. Visible `Frigo` → `Takosan`. Old Frigo palette hex values
(`#22C55E`, `#0F3D2E`, `#1ea750`, `#14532D`, `#DDF7E3`, `#FFFDF6`, `#F8FAF9`) were
mapped to Takosan tokens.

## Brand audit classification

| Reference | Class |
| --- | --- |
| index.html / manifest / sw / all page copy & logos listed above | USER_FACING_RENAME |
| `package.json` name `frigo`, `@frigo/*` aliases, D1/KV/queue bindings, API routes, migrations, event types, `X-Frigo-Expected-*` headers, `frigo_display_name` localStorage key, `/frigo/ingredients`, `/frigo/recipes`, `/api/health` `app: 'Frigo'`, harness fixture display name `Frigo Preview` | TECHNICAL_KEEP |
| `App.tsx` route comment, `frigo_frontend_prompt_kit_v1/`, docs/ai history, reports | HISTORICAL_DOC_KEEP |
| `tests/unit/logout-ux.test.ts` copy string, `tests/unit/planner-hook.test.tsx` "is planning from your data" | TEST_UPDATE (brand string only; no business assertion weakened) |

Domain: `https://frigo.tungjpstore.net` kept everywhere (OG url, export
footer). No Takosan domain invented.

## Scope guarantees

No diff under `src/worker/`, `packages/db/`, `migrations/`, `wrangler.jsonc`,
`package.json` or `pnpm-lock.yaml`. Inventory truth semantics (T09–T13), scan
confirmation, review_state, openedAt, refetch/conflict logic and the
`data-testid` / `data-item-id` / `data-expiry-kind` / `data-refetch-state`
selectors are untouched.

## Checks executed at `e37ee28`

- `git diff --check` clean; `pnpm typecheck` PASS; `pnpm lint` (scoped to
  `src/web`, tests, script) PASS; `pnpm build` PASS (dist contains
  `/takosan/*`).
- `pnpm test` (CI=1): **3480 passed / 139 files** (T13 baseline 3471/138 + 9
  new branding tests in `tests/unit/takosan-brand.test.tsx`: contract palette,
  every referenced asset exists, lockups are SVG without `<text>`, manifest,
  index.html, SW cache version, Landing/TopBar/BottomNav render Takosan and no
  Frigo logo, EmptyState pose mapping).
- Brand QA matrix (Playwright chromium, isolated preview
  `scripts/security-preview.mjs`, real synthetic login) at **360 / 390 / 430**
  over landing, onboarding, auth, home, fridge, scan, planner, profile:
  **0 broken asset requests, 0 horizontal overflow**. Screenshots in
  `.hoplite/artifacts/brandqa/` (private, not committed).
- Icons verified: 192/512 alpha PNG, cream corner pixel, coral centre; maskable
  512 composited from adaptive layers; OG 1200×630.
- `pnpm test:browser` (T13 Playwright suite, `CI=1`, serial, chromium
  360/390/430): **60 passed / 60** in 4.0 min — all T13B / T13R-A / T13R-B
  truth, ownership, conflict and presentation cases hold on the Takosan shell.

## Follow-ups (not done here)

- Remove `public/frigo/{brand,app-icons,illustrations}` and `public/assets/
  frigo-logo.svg` once brand review accepts the migration.
- Replace the remaining Lucide `Camera`/`Sparkles` in secondary surfaces with
  kit icons only where it reinforces brand; keep generic system icons.
- Dark-background lockup is used on the Splash; a dedicated dark theme is out of
  scope.
- Real replacement domain and any `/api/health` `app` rename need an explicit
  maintainer decision (technical identifier).
